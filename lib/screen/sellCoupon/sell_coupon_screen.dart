import 'package:flutter/material.dart';

class SellCouponScreen extends StatelessWidget {
  const SellCouponScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Selling coupons screen"),
    );
  }
}
